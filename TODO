Change dunder names to call foo.__class__.whatever instead of foo.whatever
Add global contains which is added to lists and tuples
Make logging sane
Remove StringLiterals pre-abstraction
Add builtin type globals to spec, and __call__ in classes to spec
Make initial objects literally empty
